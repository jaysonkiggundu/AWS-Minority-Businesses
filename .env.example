# AWS Cognito Configuration
# Get these values from your CloudFormation stack outputs
# Stack name: aws-camp-minority-businesses-dev
# Run: aws cloudformation describe-stacks --stack-name aws-camp-minority-businesses-dev --query 'Stacks[0].Outputs'
VITE_COGNITO_USER_POOL_ID=your-user-pool-id
VITE_COGNITO_USER_POOL_CLIENT_ID=your-user-pool-client-id
VITE_AWS_REGION=us-east-1

# GraphQL API (for future use)
VITE_GRAPHQL_API_URL=your-graphql-api-url
